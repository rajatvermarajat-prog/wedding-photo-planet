import { Calendar, MapPin } from 'lucide-react';
import { Project } from '@/types';

type Shoot = Project['shoots'][number] & { clientTitle: string; projectId: string };
export function UpcomingShoots({ shoots, onOpen }: { shoots: Shoot[]; onOpen: () => void }) {
  return <div className="space-y-5"><section className="dashboard-panel dashboard-panel--shoots flex flex-col overflow-hidden rounded-2xl border border-[#dfd9d2] border-t-3 border-t-[#607c96] bg-white shadow-[0_10px_30px_rgba(48,44,46,.07)]"><div className="flex items-center justify-between border-b border-[#ece6e1] bg-linear-to-r from-[#f3f7fa] to-white p-4"><h3 className="flex items-center gap-2 text-sm font-extrabold uppercase tracking-wide text-slate-800"><Calendar className="size-4 text-rose-700" />Upcoming Shoots</h3><button onClick={onOpen} className="text-xs font-bold uppercase text-rose-700 hover:underline">View Calendar →</button></div><div className="space-y-2.5 p-3.5">{shoots.length === 0 ? <p className="py-4 text-center text-xs italic text-slate-400">No upcoming shoots scheduled.</p> : shoots.map((shoot) => <div key={shoot.id} className="dashboard-shoot space-y-1 rounded-xl border border-[#e6ded8] bg-[#faf8f6] p-3 text-xs"><div className="flex items-center justify-between"><span className="font-bold text-slate-900">{shoot.title}</span><span className="rounded bg-slate-200 px-2 py-0.5 font-mono text-xs font-bold text-slate-700">{shoot.date}</span></div><p className="truncate text-xs font-medium text-slate-600">{shoot.clientTitle}</p><div className="flex items-center gap-1 text-xs text-slate-500"><MapPin className="size-3 text-slate-400" /><span className="truncate">{shoot.venue}</span></div><div className="flex flex-wrap items-center gap-x-3 gap-y-0.5 border-t border-slate-200 pt-1.5 text-xs font-medium text-slate-500">{shoot.leadPhotographer && <span>Lead: {shoot.leadPhotographer}</span>}{shoot.cinematographer && <span>Cine: {shoot.cinematographer}</span>}{shoot.droneOperator && <span className="font-bold text-rose-700">Drone: {shoot.droneOperator}</span>}</div></div>)}</div></section></div>;
}
