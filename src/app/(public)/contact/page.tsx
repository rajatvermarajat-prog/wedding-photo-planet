import { FoundationPage } from '@/components/experience/ExperienceLayouts';

export const metadata = { title: 'Contact' };

export default function ContactPage() {
  return (
    <FoundationPage
      audience="public"
      eyebrow="Public Website"
      title="Contact foundation"
      description="Placeholder route for future public inquiries that can later connect to the existing Leads module."
    />
  );
}
